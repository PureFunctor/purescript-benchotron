{
  "private": "true",
  "type": "module",
  "dependencies": {
    "benchmark": "^2.1.0",
    "microtime": "^3.0.0",
    "pulp": "^15.0.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build && pulp docs",
    "test": "pulp build -I example && (echo '*' | node example/index.js)"
  }
}
